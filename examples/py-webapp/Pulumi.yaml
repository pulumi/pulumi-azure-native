name: py-webapp
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: A minimal Azure Native Python Pulumi program
config:
  pulumi:tags:
    value:
      pulumi:template: azure-python
